[package]
name = "skilly-core-skills"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "skilly_core_skills"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

