worker/node_modules/
worker/.dev.vars
.DS_Store
*.xcuserstate
build/
releases/
.claude/
coding-plans/

# Fastlane (contains session cookies, credentials, Appfile with Apple ID)
fastlane/

# Certificates and signing (NEVER commit these)
*.cer
*.p12
*.p8
*.pem
*.key
*.certSigningRequest
*.mobileprovision
*.provisionprofile

# SkillSight local data
.skillsight/

# Secrets / env
.env
.env.*

# Xcode derived
DerivedData/
*.xcscmblueprint
xcuserdata/

# OS junk
.omx/
Thumbs.db
worker/.wrangler/

# Rust build artifacts (e.g. browser-harness daemon run from this dir)
target/
**/target/

# Tauri build-time generated permission/ACL schemas (per-platform, regenerated each build)
apps/windows-shell-gui/gen/

# Agent run-continuation tooling state
.sisyphus/

# Playwright MCP server artifacts
.playwright-mcp/
dashboard-skill-page.png

# wasm-pack output for core/web-sdk (regenerate via scripts/build-web-sdk.sh / CI)
sdk/web/generated/

# Generated CDN artifact for @skilly/web (regenerate via scripts/build-web-cdn.sh)
sdk/web-cdn-public/

# Local Netlify folder
.netlify
